Lithium is less dense than {{foo}}ium.

{% if message %}
        <div class="alert">{{ message }}</div>
{% endif %}

{% for post in posts %}
<article>
    <h1>{{post.title}}</h1>
    <p>{{post.content}}</p>
</article>
{% endfor %}
